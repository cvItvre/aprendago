# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary, build with `go teste -c`
*.test

# Output for the go coverage tool, specifically when used with LiteIDE
*.out

# Project-local glide cache, RE: https://github.com/Masterminds/glide/issues/736
.glide/
